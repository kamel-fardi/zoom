import 'package:flutter/material.dart';

class meetingsUI extends StatelessWidget {
  const meetingsUI({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('saved meetings')),
    );
  }
}
